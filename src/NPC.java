import org.newdawn.slick.geom.Point;


public class NPC extends Entity {

	public NPC(Point position, Point velocity, Point acceleration, String imageLocation) {
		super(position, velocity, acceleration, imageLocation);
	}

}
